# AI-Sentinel 项目说明

## 1. 项目整体概述 (Project Overview)

**AI-Sentinel** 是一个基于人工智能的实时视频监控与分析系统。它可接入 RTSP 摄像头或本地视频文件，利用视觉语言模型 (VLM) 对画面进行场景理解与异常检测，并将结果与元数据持久化到向量数据库 (ChromaDB) 以支持检索与回溯。系统包含低负载的时间同步、语义搜索、Web 可视化大屏等模块，支持在本地或远程 API 环境下运行。

### 核心功能 (Key Features)

1.  **多源视频接入**: 支持 RTSP 网络摄像头流和本地视频文件 (MP4/AVI) 的实时读取。
2.  **AI 实时分析**:
    *   集成 **Qwen2-VL** 等 VLM（Transformers 本地推理 / OpenAI 兼容 API / Ollama）进行场景描述和异常识别。
    *   支持自定义提示词 (Prompt) 来定义什么是"异常"。
3.  **OCR 时间戳提取**:
    *   集成 **PaddleOCR**，支持从监控画面的 OSD (On-Screen Display) 提取真实时间。
    *   **交互式区域设置**: 用户可在界面上直接框选时间戳区域，提高识别准确率。
4.  **向量存储与搜索**:
    *   **文本语义搜索**: 使用 VLM 文本描述 + 文本嵌入 API 进行语义检索。
    *   **本地双模态嵌入（可选）**: 配置本地 transformers 模型时，支持图像+文本双模态嵌入与检索。
    *   **时间筛选**: 支持按精确的时间范围过滤搜索结果。
5.  **Web 可视化大屏 (Streamlit)**:
    *   实时低延迟画面预览。
    *   实时报警滚动展示。
    *   交互式配置（VLM 参数、存储限制、分析间隔）。
    *   **数据库浏览器**: 按日期归档查看历史快照和分析记录。
6.  **智能存储管理**:
    *   支持配置最大本地图片保存数量，自动清理旧图（循环缓冲区）。
7.  **智能时间同步 (SmartTimeSync)**:
    *   通过间隔采样的 OCR + 帧插值实现低负载时间对齐，CPU 占用降低 95%+，保持毫秒级精度。

---

## 2. 项目结构 (Project Structure)

```text
aimonitor/
├── config/                 # 配置文件目录
│   └── settings.json       # 持久化配置 (JSON)
├── data/                   # 数据存储目录 (帧图片、向量数据库)
│   ├── chromadb/           # ChromaDB 持久化
│   └── frames/             # 按日期归档的帧图片
├── src/                    # 源代码目录
│   ├── analysis/           # AI 分析模块
│   │   ├── feature_extractor.py # 文本/图像嵌入 (API / Local)
│   │   ├── vector_db.py         # 向量数据库 (ChromaDB) 封装
│   │   ├── vlm.py               # 视觉语言模型 (VLM) 接口封装
│   │   └── worker.py            # 分析工作线程 (协调 VLM/Embedding/DB)
│   ├── common/             # 通用工具模块
│   │   ├── config.py            # 配置加载与管理 (JSON)
│   │   ├── queue_manager.py     # 线程安全的全局队列管理器
│   │   └── types.py             # 数据类型定义 (Frame, Alert, Result)
│   ├── ingestion/          # 视频采集模块
│   │   ├── service.py           # 采集服务 (抽帧、保存、入队、OCR)
│   │   ├── video_source.py      # 视频源实现 (File/RTSP + FFmpeg 管道)
│   │   └── smart_time_sync.py   # 低负载时间同步 (间隔OCR+插值)
│   └── webui/              # 前端展示模块
│       └── app.py               # Streamlit 主程序 (UI 逻辑)
├── requirements.txt        # 项目依赖列表
├── start.bat               # Windows 启动脚本
└── test_*.py               # 测试脚本 (采集/分析/RTSP/OCR/时间同步/嵌入)
```

---

## 3. 代码文件说明 (Code Descriptions)

### 3.1 视频采集层 (`src/ingestion`)

-   **`video_source.py`**:
    -   定义了 `VideoSource` 抽象基类。
    -   `FileVideoSource`: 使用 OpenCV 读取本地文件，支持循环播放。
    -   `RTSPVideoSource`: 使用 FFmpeg 子进程读取 RTSP 流，解决 OpenCV 读取 RTSP 易断流/花屏的问题，具备自动重连和缓冲区丢弃 (Drop-frame) 机制。

-   **`service.py`**:
    -   `VideoIngestionService`: 系统的"眼睛"。
    -   负责启动 `VideoSource` 线程读取画面。
    -   维护一个线程安全的帧队列 (`frame_queue`) 供 UI 显示。
    -   按设定的 `analysis_interval` (如每 1 秒) 抽取一帧发送给分析队列。
    -   **OCR 功能**: 初始化 PaddleOCR，包含 `extract_osd_time` 方法，根据用户设定的 ROI (感兴趣区域) 识别画面上的时间戳。
    -   管理本地图片存储，确保存储不爆满。

### 3.2 AI 分析层 (`src/analysis`)

-   **`vlm.py`**:
    -   `VLMAnalyzer`: 封装 Transformers / Ollama / OpenAI 兼容 API 三种后端；当未配置 `vlm_api_url` 时，自动选择顺序为：Ollama → Transformers → Mock（用于测试）；如需使用 OpenAI 兼容后端，请在 UI 或配置中设置 `vlm_api_url`/`vlm_api_key`/`vlm.model_name`（对应后端为 `custom`/`openai`）。
    -   支持本地推理或远程 API 调用，统一提示词模板与结果解析。
    -   生成场景描述与风险评估，并输出标准化文本结果。

-   **`vector_db.py`**:
    -   `VectorStore`: 处理基于**文本描述**的向量存储。将 VLM 生成的文本存入 ChromaDB，支持语义搜索。
    -   `VisualVectorStore`: 处理基于**文本/图像嵌入**的语义向量存储。默认使用文本嵌入；在本地模式下支持图像嵌入；提供嵌入维度一致性检测与重建。
    -   实现 `add`/`search`/`get_recent` 等方法，支持元数据过滤 (Camera ID, 时间范围)。

-   **`feature_extractor.py`**:
    -   定义 `BaseEmbedder` 抽象；`APIEmbedder`（OpenAI 兼容）用于文本嵌入；`LocalEmbedder`（transformers）支持图像+文本双模态嵌入；支持连接测试与批量嵌入。

-   **`worker.py`**:
    -   `AnalysisWorker`: 后台分析与入库管线。
    -   从 `frame_queue` 获取待分析帧，执行：VLM 分析 → 报警检测 → 智能嵌入（图像优先，失败回退文本）→ 写入向量库。
    -   将结果与报警放入 `result_queue`/`alert_queue`，并提供统计与检索接口。

### 3.3 通用模块 (`src/common`)

-   **`queue_manager.py`**:
    -   单例模式 `QueueManager`。
    -   管理 `frame_queue` (显示用), `analysis_queue` (分析用), `result_queue` (结果用), `alert_queue` (报警用)。
    -   实现了带容量限制的 `put` (自动丢弃旧数据) 以防止内存溢出。

-   **`config.py`**:
    -   基于 JSON 的配置管理。
    -   自动加载/保存 `config/settings.json`，支持设置持久化与点号访问键。

-   **`types.py`**:
    -   定义数据类 (`dataclass`)，如 `AnalysisResult`, `Alert`，确保各模块间数据传输结构统一。

### 3.4 前端交互 (`src/webui`)

-   **`app.py`**:
    -   基于 **Streamlit** 构建。
    -   **主界面**: 左侧侧边栏配置 (VLM/DB 地址)，中间显示实时视频流，右侧/下方显示实时警报。
    -   **高级设置对话框**:
        -   配置分析间隔、最大存储数量。
        -   **OCR 区域设置**: 集成 `streamlit-cropper`，允许用户在当前画面截图上框选时间戳位置。
    -   **搜索界面**:
        -   支持文本搜索和时间范围筛选 (Start/End Time)。
        -   展示搜索结果卡片。
    -   **数据库浏览器**:
        -   按日期 -> 小时 归档展示所有历史记录，方便回溯。
    -   负责系统的启动与停止 (`services.start_system()`)。

---

## 4. 依赖说明

主要依赖项见 [requirements.txt](file:///f:/aimonitor/requirements.txt):
-   Web/UI: streamlit, streamlit-cropper, requests
-   图像/数值: opencv-python, numpy, Pillow
-   向量库: chromadb
-   OCR: paddleocr / paddlepaddle（可选 EasyOCR 兜底）
-   模型: torch, transformers, accelerate, bitsandbytes
-   VLM辅助: qwen-vl-utils

## 5. 启动方式

Windows 环境下，直接运行根目录下的启动脚本：
```bat
start.bat
```
或者在激活环境后：
```powershell
conda activate microtool-ai
streamlit run src/webui/app.py
```

## 6. 配置说明

-   配置文件路径: [config/settings.json](file:///f:/aimonitor/config/settings.json)
-   关键字段:
    -   `video_source.type`: `"file"` / `"rtsp"`
    -   `video_source.path`: 视频文件路径或 RTSP URL
    -   `vlm.api_url` / `vlm.api_key` / `vlm.model_name`: VLM API 设置
    -   `embedding.provider`: `"api"` / `"local"`
    -   `embedding.api_url` / `embedding.api_key` / `embedding.model_name`
    -   `embedding.local_model_path`: 本地模型目录（绝对路径）
    -   `analysis.interval`: 抽帧分析间隔（秒）
    -   `ocr.roi`: OCR 区域比例 [x, y, w, h]
    -   `storage.max_saved_frames`: 最大本地图片保存数量

## 7. 测试与验证

-   采集服务: `python test_ingestion.py <video_path>` （见 [test_ingestion.py](file:///f:/aimonitor/test_ingestion.py)）
-   分析管线（同步/异步）: `python test_analysis.py [video_path]` （见 [test_analysis.py](file:///f:/aimonitor/test_analysis.py)）
-   RTSP 诊断: `python test_rtsp_connection.py` （见 [test_rtsp_connection.py](file:///f:/aimonitor/test_rtsp_connection.py)）
-   OCR 功能与配置: `python test_ocr_fix.py` （见 [test_ocr_fix.py](file:///f:/aimonitor/test_ocr_fix.py)）
-   智能时间同步: `python test_smart_time_sync.py` （见 [test_smart_time_sync.py](file:///f:/aimonitor/test_smart_time_sync.py)）
-   文本嵌入与语义搜索: `python test_text_embedding.py` （见 [test_text_embedding.py](file:///f:/aimonitor/test_text_embedding.py)）
-   变更快速验证: `python verify_changes.py` （见 [verify_changes.py](file:///f:/aimonitor/verify_changes.py)）

## 8. 运行时工作流

-   输入源 → 采集服务：稳定拉流、抽帧、保存帧、可选 OCR 时间识别
-   分析线程：VLM 生成描述 → 报警解析 → 嵌入生成（图像优先，失败回退文本）→ 写入 ChromaDB
-   WebUI：实时帧与 OCR 时间显示、报警滚动、两种检索模式、数据库浏览器归档回溯
-   存储管理：图片按日期归档，循环清理并同步删除向量库文档

## 9. 性能与可靠性

-   RTSP 读取通过 FFmpeg 管道，降低断流与解码异常；支持自动重连与错误日志。
-   SmartTimeSync 将 OCR 调用频率降至 ~1/75（约 1.33%），CPU 占用降低 95%+，同时保持毫秒级时间精度。
-   队列拥塞自动丢弃旧帧，保证实时性与稳定性。

## 10. 注意事项

-   如启用本地嵌入，请确保安装并配置好 `torch`/`transformers` 与相应模型目录。
-   如使用远程 Embedding 或 VLM API，请正确设置 `api_url` 与 `api_key`，并加载支持嵌入的模型（例如 `nomic-embed-text`）。
-   Windows 下需确保已安装 FFmpeg，并将 `ffmpeg`/`ffprobe` 加入 PATH。
